# ---------------------------------------------------------------------------
# 本目录的文件混合使用 CRLF 与 LF 两种行尾（程序源码与部分 CSV 为 LF，
# 其余为 CRLF）。Windows 上 core.autocrlf=true 是常见默认，会让部分文件
# 在克隆后被改写行尾，导致文件字节与原始导出不一致。
#
# 竞赛支撑材料要求文件内容与原始导出逐字节一致，因此对整个目录关闭行尾
# 转换，按二进制原样存取。
# ---------------------------------------------------------------------------

* -text
